{% extends 'base.html.twig' %}
{% block title %}Recherche{% endblock %}
{% block body %}

    <!--Recherche de grp/zicos-->
    <div class="row" align="center">
        <div class="col">
            {#<p>#}
            {#<button class="btn btn-dark searchSolo" type="button" data-toggle="collapse" data-target="#rechercheSolo" aria-expanded="false" aria-controls="rechercheSolo">#}
            {#Recherche un musicien !#}
            {#</button>#}
            {#</p>#}
            {#<div class="collapse" id="rechercheSolo">#}
            {{ form_start(searchForm, {'method': 'GET', 'attr': {'class': 'formSearch'} }) }}

            <div class="form-group">
                {{ form_row(searchForm.date_modification) }}
            </div>
            <div class="form-group">
            {{ form_row(searchForm.site) }}
            </div>
            <div class="form-group">
                {{ form_row(searchForm.quantite_stock) }}
            </div>
            <div class="form-group">
            {{ form_row(searchForm.livre) }}
            </div>
            <div class="form-group">
                {{ form_row(searchForm.submit, {'attr': {'class': 'btn btn-lg btn-dark'} }) }}
            </div>

            {{ form_end(searchForm) }}

            </div>
        </div>
    {% block resultat %}
    {% endblock %}

{% endblock %}