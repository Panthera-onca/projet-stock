{% extends 'base.html.twig' %}
{% block body %}

    <div class="titreMainPage">

        <h1>ENI Stock</h1>
        <h2>Search and Match !</h2>


        {% if is_granted('ROLE_USER') %}
            <br>
            <h4>Re-bonjour {{ app.user.username }} !</h4>
        {% endif %}
    </div>
    <div id="sidebar">
        {% block sidebar %}
            <ul>
                <li><a href="{{ path('app_searchLivre') }}">Recherches les livres</a></li>
                <li><a href="{{ path('app_recherche') }}">Recherches le stock</a></li>
            </ul>
        {% endblock %}
    </div>
{% endblock %}
